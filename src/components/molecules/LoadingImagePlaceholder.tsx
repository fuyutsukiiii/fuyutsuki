interface Props {
  loadingUrl: string;
}

const LoadingImagePlaceholder = ({ loadingUrl }: Props) => {
  return (
    <img className="h-full w-full" src />
  );
};

export default LoadingImagePlaceholder;
